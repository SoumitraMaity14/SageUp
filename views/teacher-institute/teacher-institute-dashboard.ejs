<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      display: flex;
      background: #f4f6f8;
    }
    aside {
      width: 220px;
      background: #fff;
      padding: 30px 20px;
      box-shadow: 2px 0 8px rgba(0,0,0,0.05);
      height: 100vh;
    }
    aside nav a {
      display: block;
      text-decoration: none;
      padding: 10px;
      border-radius: 8px;
      color: #333;
      margin-bottom: 10px;
    }
    aside nav a.active, aside nav a:hover {
      background: #e6f0ff;
      color: #007bff;
    }
    .main-content {
      flex: 1;
      padding: 40px;
    }
  </style>
</head>
<body>

  <!-- Sidebar Tabbar -->
  <aside>
    <div style="display:flex; flex-direction:column; align-items:center; margin-bottom:30px;">
  <div style="display: flex; align-items: center; gap: 10px;">
    <img src="/images/icon.png" alt="Admin Icon" style="max-width: 150px;height: 50px;">
  </div>
</div>

    <nav>
      <a href="/dashboard/<%= user._id %>/home" class="<%= page === 'home' ? 'active' : '' %>">🏠 Home</a>
      <a href="/dashboard/<%= user._id %>/course" class="<%= page === 'course' ? 'active' : '' %>">📚 Course</a>
      <a href="/dashboard/<%= user._id %>/libraries" class="<%= page === 'libraries' ? 'active' : '' %>">🏛 Libraries</a>
      <a href="/dashboard/<%= user._id %>/review" class="<%= page === 'review' ? 'active' : '' %>">📝 Review</a>
      <a href="/dashboard/<%= user._id %>/application" class="<%= page === 'application' ? 'active' : '' %>">📄 Application</a>
      <a href="api/users/logout">🚪 Logout</a>
    </nav>
  </aside>

  <!-- Dynamic Main Content -->
  <div class="main-content">
    <%- include(`Tabs/${page}`, {user, subjects, editMode, libraries}) %>
  </div>

</body>
</html>
